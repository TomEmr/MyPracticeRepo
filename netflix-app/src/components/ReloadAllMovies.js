
const ReloadAllMovies = (props) => {
    return (
        <div>
            <button className="main-reload-button" onClick={props.reloadAllMovies}>Reload All Movies</button>
        </div>
    );
}

export default ReloadAllMovies;